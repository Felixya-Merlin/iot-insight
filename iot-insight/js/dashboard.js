/*
 * dashboard.js
 *
 * Dashboard-specific functionality.
 *
 * No fake data is created here.
 */

document.addEventListener("DOMContentLoaded", () => {

    console.log(
        "Dashboard initialized."
    );


    /*
     * Future:
     *
     * GET /api/dashboard/summary
     *
     * Then update:
     *
     * Connected Devices
     * Active Parameters
     * Readings Received
     * Active Alerts
     *
     * And load real readings into Chart.js.
     */

});